/**
 * WASM formatter for Python using Ruff.
 *
 * @example
 * ```ts
 * import { format } from "@wasm-fmt/ruff_fmt";
 *
 * const input = "def foo ():\n    return 42";
 * const output = format(input);
 * ```
 *
 * @module
 */
